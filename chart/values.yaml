replicaCount: 2

image: morozovav82/order:1.0

service:
  type: NodePort
  port: 9000

appBasePath: '/order'

postgresql:
  fullname: users-postgresql
  postgresqlDatabase: users
  postgresqlUsername: users
  postgresqlPassword: qwerty123
  service:
    port: "5432"

mq:
  url: mq-artemis
  port: 61616
  user: artemis
  password: password

ingress:
  enabled: true
  hosts:
    - host: arch.homework
      paths: ["/order"]
      
metrics:
  serviceMonitor:
    enabled: false